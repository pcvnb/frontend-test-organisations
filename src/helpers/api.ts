const baseAddress = 'https://raw.githubusercontent.com/arkdich/mybuh-frontend-test/main';

export const API = {
  orgs: `${baseAddress}/companies.json`,
  ownerships: `${baseAddress}/ownerships.json`,
  taxSystems: `${baseAddress}/tax-systems.json`,
  FormsToSystems: `${baseAddress}/form-to-system.json`,
};
